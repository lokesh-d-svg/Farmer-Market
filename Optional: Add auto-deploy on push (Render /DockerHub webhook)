If using Render, auto-deploy is built-in when you connect the repo.

If using Docker Hub, configure a webhook from Docker Hub to your host or use GitHub Actions to push images and SSH to your server to docker pull && docker-compose up -d.
